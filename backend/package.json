{
  "name": "rufare-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "audit:public-profiles": "node scripts/audit-public-profiles.cjs",
    "cleanup:public-profiles": "node scripts/audit-public-profiles.cjs --apply --confirm-cleanup",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase": "^12.14.0",
    "firebase-admin": "^13.10.0"
  },
  "overrides": {
    "uuid": "^11.1.1"
  },
  "engines": {
    "node": ">=18"
  }
}
